name: Build and Push Production Image

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: chisamuela/chisamuel-v2
  NEXT_PUBLIC_SERVER_URL: https://chisamuel.com

jobs:
  # ==========================================
  # JOB 1: Environment & Dependency Setup
  # ==========================================
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

  # ==========================================
  # JOB 2: Code Quality & Type Checks (Sequential: Runs AFTER Setup is complete)
  # ==========================================
  check:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Linting
        run: pnpm run lint

      - name: Run TypeScript typechecking
        run: pnpm exec tsc --noEmit

  # ==========================================
  # JOB 3: Integration & Unit Tests (Sequential: Runs AFTER check is 100% complete)
  # ==========================================
  test:
    runs-on: ubuntu-latest
    needs: check
    # Spin up MongoDB service to handle test-time database queries
    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Tests
        env:
          # Use local MongoDB service container on port 27017 for integration tests
          DATABASE_URL: mongodb://localhost:27017/test-db
          # Use a self-contained placeholder secret for the test run
          PAYLOAD_SECRET: a_temporary_test_secret_key_to_pass_validation_12345
          NODE_ENV: test
        run: pnpm run test:int

  # ==========================================
  # JOB 4: Compile Production Build (Sequential: Runs AFTER test is 100% complete)
  # ==========================================
  compile:
    runs-on: ubuntu-latest
    needs: test
    # Spin up MongoDB service to handle build-time static page queries
    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Next.js App
        env:
          # Use local MongoDB service container on port 27017
          DATABASE_URL: mongodb://localhost:27017/build-db
          # Use a self-contained placeholder secret
          PAYLOAD_SECRET: a_temporary_build_secret_to_pass_compilation_12345
          NEXT_PUBLIC_SERVER_URL: ${{ env.NEXT_PUBLIC_SERVER_URL }}
          NODE_ENV: production
          NEXT_TELEMETRY_DISABLED: 1
        run: pnpm run build

      - name: Upload Standalone Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: standalone-build
          retention-days: 1
          path: |
            .next
            public
            !.next/cache

  # ==========================================
  # JOB 5: Package & Push Docker Image (Sequential: Runs AFTER compile is 100% complete)
  # ==========================================
  publish:
    runs-on: ubuntu-latest
    needs: compile
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Standalone Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: standalone-build
          path: .

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.prod
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
